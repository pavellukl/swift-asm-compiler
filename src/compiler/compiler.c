#include "compiler.h"

// CompilerReturnValue compile(const FILE *in, const FILE *out,
//                             const CompileParams params) {
//     assert(in != NULL);
//     assert(out != NULL);

//     return COMP_OK;
// }